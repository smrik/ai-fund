# Legacy Rollout Plan

This document was previously published as `docs/PLAN.md`.

It is archived because `docs/PLANS.md` is now the canonical repository guidance file and `docs/plans/index.md` is the canonical plan registry.

The original content is retained below for historical reference.

