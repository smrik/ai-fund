# Active Plans

This directory contains canonical plans that are currently being executed.

Current active plan:

- No active canonical plans at the moment.

When starting a new major workstream, add the canonical plan here and register it in [../index.md](../index.md). Move plans to `../completed/` or `../archive/` once they stop being the active source of truth.
